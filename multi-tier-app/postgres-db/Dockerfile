# Use the official PostgreSQL 13 image as the base image
FROM postgres:13

# Set environment variables to configure the PostgreSQL database
ENV POSTGRES_DB=mydb
ENV POSTGRES_USER=myuser
ENV POSTGRES_PASSWORD=mypassword

